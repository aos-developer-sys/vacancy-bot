# secrets
.env

# Telegram session files (equivalent to auth tokens - never commit)
*.session
*.session-journal

# HH.ru OAuth access token cache - equivalent to an auth token, never commit
hh_token_cache.json

# runtime state / data
*.sqlite3
*.log

# python
__pycache__/
*.pyc
venv/

# local Claude Code instructions - не относится к самому проекту, не в репозитории
CLAUDE.md
